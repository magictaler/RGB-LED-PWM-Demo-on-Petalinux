#
# This file is the bootscript recipe.
#

SUMMARY = "Simple bootscript application"
SECTION = "PETALINUX/apps"
LICENSE = "MIT"
LIC_FILES_CHKSUM = "file://${COMMON_LICENSE_DIR}/MIT;md5=0835ade698e0bcf8506ecda2f7b4f302"

SRC_URI = "file://bootscript \
	"

S = "${WORKDIR}"

do_install() {
	     install -d ${D}/${bindir}
    install -m 0755 ${S}/bootscript ${D}/${bindir}


             # move runtime scripts to ~/ptp_scripts
  install -d ${D}/home/root/httpd
  install -m 0755 web/baraja.css ${D}/home/root/httpd
  install -m 0755 web/tabs.css ${D}/home/root/httpd
  install -m 0755 web/footer.html ${D}/home/root/httpd
  install -m 0755 web/header.html ${D}/home/root/httpd
  install -m 0755 web/index.html ${D}/home/root/httpd
  install -m 0755 web/config.js ${D}/home/root/httpd
  install -m 0755 web/jquery-1.12.4.min.js ${D}/home/root/httpd
  install -m 0755 web/BarajaLogo.png ${D}/home/root/httpd
  install -m 0755 web/Averta-stripped.woff ${D}/home/root/httpd
}
